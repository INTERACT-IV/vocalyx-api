# =====================================================================
# Vocalyx API - Configuration File
# =====================================================================

[DATABASE]
# URL de connexion PostgreSQL
# Format: postgresql://user:password@host:port/database
url = postgresql://postgres:toto@192.168.1.234:5432/vocalyx_db

[REDIS]
# URL de connexion Redis (pour Celery et cache)
url = redis://192.168.1.235:6379/0

[CELERY]
# Configuration Celery
broker_url = redis://192.168.1.235:6379/0
result_backend = redis://192.168.1.235:6379/0

[PATHS]
# Répertoire partagé pour les uploads (DOIT être partagé avec les workers)
upload_dir = /home/shinohk/code/vocalyx-all/shared/uploads

[SECURITY]
# Clé secrète pour la communication inter-services (Dashboard <-> API <-> Workers)
# ⚠️ CHANGER CETTE VALEUR EN PRODUCTION
internal_api_key = SuperSecretCode123

# Nom du projet administrateur (pour la gestion des projets)
admin_project_name = ISICOMTECH

jwt_secret_key = CHANGE_ME_SUPER_SECRET_KEY_FOR_JWT_123456
jwt_algorithm = HS256
jwt_expire_minutes = 10080

[CORS]
# Origins autorisées pour CORS (séparées par des virgules)
# Exemples: http://localhost:8080,https://dashboard.vocalyx.com
origins = http://localhost:8080,http://localhost:3000

[LOGGING]
# Niveau de log: DEBUG, INFO, WARNING, ERROR, CRITICAL
level = INFO

# Activer les logs dans un fichier
file_enabled = true

# Chemin du fichier de log
file_path = /home/shinohk/code/vocalyx-all/shared/logs/vocalyx-api.log

# Activer les couleurs dans la console
colored = true

[LIMITS]
# Taille maximale des fichiers uploadés (en MB)
max_file_size_mb = 100

# Extensions de fichiers autorisées (séparées par des virgules)
allowed_extensions = wav,mp3,m4a,flac,ogg,webm

[TRANSCRIPTION]
# Durée minimale (en secondes) pour activer le mode distribué
# Les fichiers audio plus longs que cette valeur seront automatiquement découpés
# et distribués sur plusieurs workers pour traitement parallèle
# Peut être surchargé par la variable d'environnement DISTRIBUTED_MIN_DURATION_SECONDS
distributed_min_duration_seconds = 30